# Ledger (Double-Entry) — Fastify + TypeScript

## Running local

```bash
npm ci
npm run dev
```

## Tests

```bash
npm test
```

## Enpdoints

- `POST /accounts`
- `GET /accounts/:id`
- `POST /transactions`
